using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NumCoinsText : MonoBehaviour
{

    //text seems to move automatically with camera because camera is its parent class? code below not necessary.
    //text update controlled via CoinInfo

    // Update is called once per frame
    // void LateUpdate()
    // {
    //     Transform target = Camera.main.transform;
    //     transform.SetParent(transform.parent, false);
    // }



}
